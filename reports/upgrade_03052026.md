# Upgrade Report — 03 May 2026

**Scope:** `src/text_based/nemotron3/` and its test suite (`src/text_based/testing/test_text_based.py`)
**Trigger:** `/upgrade-repo` after adding `hf_sft_trainer.py`, `unsloth_trainer.py`, `fewshot_prompter.py`

---

## Summary

12 issues found and resolved. No structural reorganisation was needed — directory layout was already correct. All fixes are surgical; no logic was rewritten beyond what was broken.

---

## Changes Applied

### Critical Bug Fixes

| # | File | Issue | Fix |
|---|------|-------|-----|
| 1 | `fewshot_prompter.py` | `_extraction_candidates`: `end` sentinel initialised to `start` instead of `-1`. If model output has `{` but no `}`, a single-character garbage slice was appended as a parse candidate. | `depth, end = 0, -1`; added `if end != -1:` guard before `append`. |
| 2 | `run_demo.py` | Same brace-extraction bug (copy-pasted code). | Same fix. |
| 3 | `inference.py` | `generate_raw()` called `self.model.generate(...)` with no guard. Invoking without `load_model()` raised unhelpful `AttributeError: 'NoneType'...`. | Added `if self.model is None: raise RuntimeError(...)` at top of `generate_raw()`. |
| 4 | `inference.py` | `predict()` called `_repair()` and immediately returned `repaired=True` without verifying repair actually restored connectivity. | Added `"fully_connected": self._is_connected(data)` to the return dict after repair. |

### Minor Bug Fixes

| # | File | Issue | Fix |
|---|------|-------|-----|
| 5 | `fewshot_prompter.py` | Integer division `//` in summary percentage — e.g. 66.67% printed as `66 %`. Inconsistent with `evaluate.py` which uses float `/`. | Replaced with `pct = (100.0 * len(valid) / total) if total else 0.0` + `{pct:.1f}`. |
| 5 | `run_demo.py` | Same integer division bug. | Same fix. |
| 6 | `evaluate.py` | British spelling `"No samples to visualise."` inconsistent with rest of codebase. | Changed to `"visualize"`. |

### Redundancy Removed

| # | File | Issue | Fix |
|---|------|-------|-----|
| 7 | `unsloth_trainer.py` | `_LORA_R = 16` and `_LORA_ALPHA = 32` defined as module-level constants, shadowing `Config.LORA_R` / `Config.LORA_ALPHA`. A change to `Config` would silently diverge. | Removed the two module-level constants; replaced all three usages (`_load_with_unsloth`, `_load_with_peft`, `save`) with `self.cfg.LORA_R` / `self.cfg.LORA_ALPHA`. |

### Debugging Improved

| # | File | Change |
|---|------|--------|
| 11 | `inference.py` | `predict()` retry loop now prints per-attempt failure reason: `[attempt N] parse failed: ...` / `[attempt N] structure check failed: ...`. Previously all failures were silent. |

### Test Coverage Added

| # | Test class | Tests added | What they verify |
|---|-----------|-------------|-----------------|
| 8 | `TestNemotron3Files` | Updated `_SCRIPTS` list | `hf_sft_trainer.py`, `unsloth_trainer.py`, `fewshot_prompter.py` all exist on disk |
| 9 | `TestNemotron3ClassImports` (new, tests 38–40) | Import smoke tests | Each file importable; class name (`HFSFTTrainer`, `UnslothTrainer`, `FewShotPrompter`) present |
| 10 | `TestFewShotPrompterLogic` (new, tests 41–45) | Pure-Python repair/parse | `_repair_lines` connects isolated points; skips when Lines present; `_try_parse` succeeds on valid JSON / returns None on garbage; `_extraction_candidates` does not crash or emit a garbage slice on unmatched braces |

**Test run result:** 11/11 passed, 0 failures, 0 errors.

---

## Deferred / Not Changed

| Item | Reason deferred |
|------|----------------|
| Extract shared JSON utilities into `json_utils.py` | `fewshot_prompter.py`, `run_demo.py`, and `inference.py` share similar logic. Extracting it is a valid refactor but involves more touching of files and test changes than a bug-fix warrants. Flagged for a future dedicated refactor PR. |
| `_bnb_config()` duplication across 4 files | Same rationale — the duplication is harmless and each copy has subtle differences (GPU vs CPU dtype). Safe to leave. |
| Dataset CSV structure validation in `dataset_loader.py` | Would add runtime validation at train-time; out of scope for this pass. |

---

## Files Modified

```
src/text_based/nemotron3/fewshot_prompter.py    — fixes 1, 5
src/text_based/nemotron3/run_demo.py            — fixes 2, 5
src/text_based/nemotron3/inference.py           — fixes 3, 4, 11
src/text_based/nemotron3/evaluate.py            — fix 6
src/text_based/nemotron3/unsloth_trainer.py     — fix 7
src/text_based/testing/test_text_based.py       — fixes 8, 9, 10
```
