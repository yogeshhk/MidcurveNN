# Upgrade Report — 02 August 2026

**Scope:** `/upgrade-repo-tech` review, 6th pass (see `reports/upgrade_13072026.md` for the 5th).
Focused on what's new since that pass: this session also fixed pix2pix's 3 code-level bugs
(logged in `src/image_based/analysis_report.md` and `CLAUDE.md` directly, not duplicated here).
**Not re-proposed:** `CODE_OF_CONDUCT.md` / issue-PR templates and em-dash cleanup in the files
listed under "Declined" in the Jul 13 report — the maintainer already reviewed and passed on
these; not raising them again.

---

## Summary

6 items approved by the maintainer in one batch and applied. 3 items surfaced as flags requiring
a separate maintainer decision (per this skill's personal-data and scope-flag guardrails) and are
awaiting an answer, not yet acted on. No git commands were run (repo owner manages all git
operations); changes are staged in the working tree only.

---

## Changes Applied

### Code Fixes (Critical Issues)

| # | Item | Fix |
|---|------|-----|
| 1 | pix2pix generator/data range mismatch (`pix2pix/keras_gan_pix2pix.py:112`) | Changed final activation from `sigmoid` to `tanh`, matching the DataLoader's `[-1,1]` normalization and the existing `0.5*x+0.5` display rescale in `sample_images`. |
| 2 | pix2pix crash: `astype(np.float)` (`pix2pix/keras_gan_data_loader.py:96`) | Changed to `astype(float)`. `np.float` was removed in NumPy 1.24, which `environment.yml` pins. |
| 3 | pix2pix `load_batch` off-by-one (`pix2pix/keras_gan_data_loader.py:63`) | Changed `range(self.n_batches-1)` to `range(self.n_batches)`. |
| 4 | `src/image_based/analysis_report.md` | Marked bugs 1, 2, 10 fixed with dates; updated the pix2pix summary-table row; added a design-level caveat (moved from README, see below) noting the GAN-vs-exact-loss suitability question survives the bug fixes. |
| 5 | `CLAUDE.md` | Updated the stale "no code changes applied" note for the three `analysis_report.md` files to reflect pix2pix's fixes. |

Verification: syntax-checked via `py_compile` (no `midcurvenn` conda env on this machine to run
end-to-end; flagged to the maintainer as a follow-up if a real training smoke test is wanted).

### Structural Changes

| # | Item | Action | Reason |
|---|------|--------|--------|
| 6 | `README.md` "references/" paragraph | Fixed | Described a top-level `references/` folder that no longer exists; its `prompts/` subfolder is now `publications/prompts/`. Updated the paragraph and folder-count wording accordingly. |
| 7 | `README.md` dead HTML-commented block (~55 lines) | Trimmed | Stale scratch content invisible to readers: old training-data-prep notes, outdated Keras-install notes, pix2pix reference links, a "Pending" list duplicating `TODO.md`, and personal "Why Me?" motivation notes — all deleted. Two pieces preserved: environment-setup gotchas moved into `CLAUDE.md` Known Gotchas; the "Why Pix2Pix may not be good" design reasoning moved into `src/image_based/analysis_report.md` as a caveat on the pix2pix section (still relevant after today's bug fixes). |
| 8 | `publications/Midsurface_PhDThesis/publications/TwoPagerNotes/Presentation_defense.pptx` | Deleted | Byte-identical duplicate (verified via MD5) of `.../Presentation/Presentation_defense.pptx`. 14MB reclaimed. |
| 9 | `README.md` Publications/Talks section | Moved | Was positioned after the repo-structure tree, run instructions, and a long "Thoughts" essay. Moved to right after the intro hook/image — it's the repo's strongest credibility signal (2 peer-reviewed papers, IEEE Xplore, a Google Developers Dev Library feature, Kaggle datasets) and was easy to miss where it sat before. Internal anchor link (`#publicationstalks`) still resolves correctly at the new position. |
| 10 | `README.md` "no code changes applied yet" line | Fixed | Now notes pix2pix's fixes explicitly rather than reading as blanket-still-open. |

---

## Awaiting Maintainer Decision (flagged only, not acted on)

| # | Item | Options |
|---|------|---------|
| 11 | `publications/IntroToGeoMod/` legacy `.ppt`/`.pdf` snapshots (`Intro_3DModeling`, `Intro_SolidModeling`, `Introduction To 3D Modeling_yhk`, ~33MB combined) | Appear superseded by the actively-maintained `Main_Intro_GeoMod_Comprehensive.tex` pipeline in the same folder. Not byte-identical to each other, so not auto-removed. Archive, keep as historical snapshots, or delete? |
| 12 | `README.md` Disclaimer section | Publicly lists the maintainer's personal email as contact. Per this skill's guardrail, personal-data flags are never auto-actioned. May be an intentional contact line; keep as-is, or swap for a GitHub-based contact? |
| 13 | GitHub repo Description/Topics (manual, web UI only) | A step-by-step guide was delivered on 2026-07-13 but not confirmed done. Is it set now? |

---

## Deferred / Out of Scope

Same as the Jul 13 report: the ~50 remaining code-level bugs across the three `analysis_report.md`
files (image/geometry/text) are pre-existing, already severity-ranked in their own files, and out
of scope for a structure/discoverability/hygiene-plus-one-bugfix-set pass. Highest shared priority
across all three: dataset splitting doesn't group augmented variants by base shape, so reported
accuracy likely reflects interpolation rather than generalization.

---

## Files Modified

```
src/image_based/pix2pix/keras_gan_pix2pix.py       — sigmoid -> tanh activation fix
src/image_based/pix2pix/keras_gan_data_loader.py    — np.float crash fix, off-by-one fix
src/image_based/analysis_report.md                  — 3 bugs marked fixed, design caveat added
CLAUDE.md (MidcurveNN root)                         — stale fix-status note updated, gotchas added
README.md                                            — references/ paragraph, dead block trim,
                                                         Publications/Talks moved, stale line fixed
TODO.md                                              — this session's checklist, stale references/
                                                         mention removed, pix2pix status updated
```

## Files Removed

```
publications/Midsurface_PhDThesis/publications/TwoPagerNotes/Presentation_defense.pptx
```

---

## Next Steps for the Maintainer

1. Answer the 3 flagged items above (legacy IntroToGeoMod files, README email, GitHub Topics).
2. Review the working-tree diff.
3. Commit — the agent does not run git commands; grouping into logical commits (code fix, docs
   cleanup, asset removal) is recommended but up to you.
4. Optional: set up the `midcurvenn` conda env and run pix2pix end-to-end to confirm the fix
   trains without error (only syntax-checked so far, not runtime-verified).
